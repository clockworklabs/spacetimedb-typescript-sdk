{
  "name": "spacetimedb-ts-sdk",
  "private": true,
  "scripts": {
    "compile": "pnpm -r compile",
    "format": "prettier --write .",
    "lint": "prettier . --check"
  },
  "devDependencies": {
    "prettier": "2.8.7",
    "terser": "^5.31.2",
    "tsup": "^8.1.0",
    "typescript": "^5.5.3",
    "vitest": "^2.0.3"
  }
}
